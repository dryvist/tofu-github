# SOPS creation rule: every file under secrets/ encrypts to the fleet age
# recipient (private key per-host at ~/.config/sops/age/keys.txt — a path,
# never a committed value). Mirrors the fleet pattern (iac-platform,
# terraform-proxmox).
creation_rules:
  - path_regex: .*
    age: age1y0jvtlp2ac8fwwvh2nzm88ehye74jlseywjrjcjqll7u8vycsedq788tp9
