# MANAGED BY tofu-github (config/workflows/issue-sweeper.yml) — do not edit in the repo.
# Weekly: scan open issues, close ones with a merged PR, flag stale/in-progress.
name: Issue Sweeper

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 0"

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: read

jobs:
  sweep:
    uses: dryvist/ai-workflows/.github/workflows/issue-sweeper.yml@v0
    secrets: inherit # zizmor: ignore[secrets-inherit]
